import { PhotoSortPipe } from './photo-sort.pipe';

describe('PhotoSortPipe', () => {
  it('create an instance', () => {
    const pipe = new PhotoSortPipe();
    expect(pipe).toBeTruthy();
  });
});
