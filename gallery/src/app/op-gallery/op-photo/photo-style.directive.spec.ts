import { PhotoStyleDirective } from './photo-style.directive';

describe('PhotoStyleDirective', () => {
  it('should create an instance', () => {
    const directive = new PhotoStyleDirective();
    expect(directive).toBeTruthy();
  });
});
