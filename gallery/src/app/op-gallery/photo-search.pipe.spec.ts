import { PhotoSearchPipe } from './photo-search.pipe';

describe('PhotoSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new PhotoSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
